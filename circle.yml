machine:
  python:
    version: python-2.7.6
  services:
    - docker

dependencies:
  override:
    - pip install -r requirements.txt

test:
  override:
    - bin/py.test src/eavatar.hub/tests/unit


